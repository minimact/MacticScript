// Module imports
const { classify_node, is_static, is_hybrid, Dependency } = require('../../../../rustscript-plugin-minimact/analyzers/classification.js');

// Generated by RustScript compiler
// Do not edit manually

module.exports = function({ types: t }) {
  
  function test_classify_static_node() {
    const deps = new Set();
    const classification = classify_node(deps);
    const is_static_result = is_static(deps);
  }
  
  function test_classify_client_node() {
    let deps = new Set();
    deps.add({ name: "useState", dep_type: "client" });
    deps.add({ name: "count", dep_type: "client" });
    const classification = classify_node(deps);
    const is_static_result = is_static(deps);
  }
  
  function test_classify_server_node() {
    let deps = new Set();
    deps.add({ name: "userId", dep_type: "server" });
    deps.add({ name: "dbData", dep_type: "server" });
    const classification = classify_node(deps);
  }
  
  function test_classify_hybrid_node() {
    let deps = new Set();
    deps.add({ name: "count", dep_type: "client" });
    deps.add({ name: "userId", dep_type: "server" });
    const classification = classify_node(deps);
    const is_hybrid_result = is_hybrid(deps);
  }
  
  let state = {};
  
  return {
    visitor: {

    }
  };
};
